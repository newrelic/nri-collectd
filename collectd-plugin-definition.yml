name: com.newrelic.collectd-plugin
description: Reports status and metrics for collectd-plugin service
protocol_version: 2
os: linux

commands:
  metrics:
    command:
      - ./nri-collectd
      - -metrics
    interval: 120
